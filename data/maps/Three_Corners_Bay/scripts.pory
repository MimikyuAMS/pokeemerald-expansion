mapscripts Three_Corners_Bay_MapScripts {
MAP_SCRIPT_ON_TRANSITION: Three_Corners_Bay_On_Transition
}


script Three_Corners_Bay_On_Transition {
    if (flag(FLAG_CAUGHT_RAIKOU) && flag(FLAG_CAUGHT_ENTEI) && flag(FLAG_CAUGHT_SUICUNE) == TRUE){
        removeobject(1)
        setflag(FLAG_LEGENDARY_BEASTS_CAUGHT_TRAP)}
        end
}
script RocketGuard {
    lockall
    faceplayer
    msgbox("Scram Kid!")
    waitmessage
    releaseall
}